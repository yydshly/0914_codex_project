# 网页构建与检查记录

日期：2026-09-16。网页位于本子项目 `web/`，沿用总仓库原生静态页面结构。

## 已完成

- 四个阅读入口：来源目录、三家对比、数据处理、证据与边界。
- 来源搜索、平台/类别/状态筛选、规划项目开关、分页与详情对话框。
- 九类数据清理说明，可切换查看建议处理、验收重点与理解示例。
- 148 条登记与 19 个证据页面由现有 JSON 构建，避免在网页重复维护另一份清单。
- 原始资料下载、证据外链、手机布局、键盘焦点样式及原生对话框关闭行为均已编写。

## 实际检查

- 构建时检查 HTML 本地引用、锚点、标签关联、唯一 ID 与资料引用。
- JavaScript 源码与生成数据文件通过语法检查。
- 筛选逻辑检查通过：默认 73 条；包含规划后 148 条；AIsa 规划记录 79 条；Semrush 状态冲突；小红书英文别名；空结果；平台范围条件。
- 本地 HTTP 入口返回 200，已请求在 Codex 打开本地网页。
- 总目录检查通过，共 13 个子项目。

没有执行浏览器截图、DOM 检查或点击测试；不将代码与逻辑检查表述为视觉验收。未调用三家业务接口。

## 交付边界

当前为本地可阅读网页，未发布外网，未更改已上线演示的发布清单。构建无需联网，外部证据链接需联网。完整产物可由 `src/build_site.py` 重建。

## 获取方式补充检查

- 增加“获取方式”第五个入口；148 条记录按接入商分别保存调用方式、确认程度、授权条件、原始数据获取方式、RSS 状态和未知项。
- 官方证据增至 25 页；24 页有本地快照，FRED 通过网页阅读工具核查，本地下载超时已单独记录。
- API 与 MCP 不混为原始平台官方 API；AgentKey 具体 REST 路径保留待核；AIsa 未逐来源确认的 MCP 不生成“已映射”标签。
- TikHub MCP 的 others 分组映射到文档列出的 Lemon8、皮皮虾、西瓜视频和今日头条；Telegram 未找到明确 MCP 映射。
- 筛选检查通过：默认73条、包含规划148条；TikHub 数据集来源11条、扩展来源5条；RSS/Atom 原站路径仅标 YouTube，官方直连 API 示例仅标 FRED；规划条目不自动成为 API。
- 原站直连单独呈现，不算作三家转发该协议的证据。
- 本轮构建引用与 JavaScript 语法检查通过，本地入口返回200。未增加浏览器交互或真实业务 API 实测。

## 接入明确程度修正

- 将对外接入文档、后台采集机制、实际调用验证分开，移除混合来源行上的笼统“部分入口待核”提示。
- 来源去重后按最明确的接入商证据归类：46条已有来源级接口目录、26条统一入口/来源待定位、1条状态冲突、75条仅规划。
- 添加接入明确程度筛选、只看明确入口按钮和明确入口优先排序；选平台后按该平台自身证据判断，不借用其他接入商的结论。
- 给出既有文档中的三个请求入口示例：TikHub TikTok 视频资料、AIsa Firecrawl 正文提取、AIsa Jina 排序；没有执行请求。
- 供应商后台采集与未核实的 RSS 等补充信息默认折叠，仍可展开查看。

## 摘要、引导图与发布准备（2026-09-16）

- 增加默认研究导读、完整文字摘要、三家分工、可点击分类名录及 imagegen 生成的引导图；73 条非“仅规划”的名称按九类展示，处理与操作工具单列。
- 目录保持 148 条，规划项默认隐藏；图中只放代表名称，不把供应商覆盖数相加。
- 本轮浏览器检查了导读图文排版、73 条分类名录、TikTok 详情及具体 API 示例、AIsa 跳转后包含规划的 111 条结果。检查中修复了原有 AgentKey 下拉选项缺失起始标签的问题。
- 构建通过本地引用检查，JavaScript 语法和筛选数量检查通过；统一站点成功打包 13 个项目。
- 本节更新此前“仅本地预览”的交付状态：已接入 GitHub Pages 发布流程，正式在线结果以随后部署记录为准。此次仍未调用三家业务 API。
