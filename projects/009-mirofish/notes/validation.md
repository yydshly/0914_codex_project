# 检查记录 · 2026-09-16

## 能力全景图增补

新增 1800×2480 的 `mirofish-capabilities.svg` 与 PNG，汇总五类场景、三类输入、内部处理循环、六种呈现方式及价值边界。明确区分官方舆情 / 红楼梦演示与可设计用途；标记模型、OASIS、Zep、MiroFish 和 ReportAgent 的分工。SVG 内含固定版本源码链接。

在 Chrome 中测量 130 个文本元素，均未超出各自宽度预算；查看最终图片，回路箭头与文字无交叠。网页总览已更新，17 文件构建引用检查通过；图片加载尺寸正确，1440 / 390 / 320 像素页面无横向溢出。原先构建与使用流程图保留链接。图为源码归纳，不是实际模拟结果。

## 对话与信息流图增补

互动模拟增加三种同步视角：事件时间轴、角色对话、信息流图。对话及图节点可点选，展示身份关注点、所见信息、行动和下一轮可见内容。六个教学分支显式声明引用关系；故事保密分支的两条私人记录单独标记，不向其他角色传播。解释为作者注释，不是模型内部思考，也不是实际推演日志。

浏览器检查覆盖三个场景 × 两种情景 × 两个新增视角、键盘选中记录、轮次截断与选中状态同步、私人信息标记，以及 1440 / 900 / 390 / 320 像素宽度；页面无横向溢出，信息流图在窄屏内部滚动。原有时间轴与访谈冒烟检查通过，无浏览器运行错误。查看桌面信息流图和手机对话截图，文字与布局正常。JavaScript 语法与 15 文件构建检查通过。此次仍未调用模型。

## 全流程图增补

新增可编辑 SVG 与高清 PNG，解释运行环境、使用场景、资料输入、图谱、角色配置、互动循环和输出。核对固定版本中 `OasisAgentProfile`、`user_char`、`ModelFactory.create` 与角色图生成调用，确认多 Agent 不要求多种模型。图示不宣称执行了真实推演。

已查看 1600×1830 图像，无文字遮挡；构建增加到 15 个公开文件，引用检查通过；浏览器确认图像完整加载，320 像素页面无横向溢出。窄屏可点击查看可缩放原图。

## 真实案例增补

新增 Unity 历史案例、三种候选方案切换及两份实验资料下载。更新后的构建包含 13 个公开文件。运行 `src/verify_real_case.cjs`，22 项检查通过，记录见 `real-case-ui-checks.json`：覆盖方案选择、键盘操作、下载文件内容、四种页面宽度和原有教学时间轴 / 访谈回归。已查看真实案例桌面截图，历史事实与实验假设有明确标识。

页面资料来源包括 2023-09-13 开发者公开文章、2023-09-22 Unity 官方修订公开信、2024-09-12 官方取消公告。后续事实没有写入 `unity-seed.md`；当前模型可能已知道历史结局的限制已在实验设计中记录。此次没有调用真实模型。

## 已完成

- JavaScript 语法检查：`main.js` 与 `scenarios.js` 通过。
- 展示构建：10 个白名单文件，检查本地资源、章节锚点、重复 ID 和意外产物，通过。
- 项目索引：9 个研究项目的目录与首页登记检查通过。
- 总站打包：9 个已选演示成功打包到本项目忽略目录 `.cache/pages-check/`；没有部署。
- Chrome 无界面浏览器实际访问本地展示页，165 项检查通过，详情见 [ui-checks.json](ui-checks.json)。
- 三类案例 × 两种情景均核对互动记录和报告下载，下载文件包含教学数据声明。
- 检查 18 个角色的三个预设访谈问题，在两种情景下均与预设内容一致。
- 验证角色图键盘选择、前后步骤、播放、暂停与重置。
- 在 1440、900、390、320 像素宽度检查横向溢出与访谈可用性。
- 页面图片全部加载，无浏览器运行异常或 HTTP 错误；运行时没有发起外部请求。
- 查看桌面与手机长截图，页面层级、关系图、原版截图和页尾来源均正常显示。

## 复查方法

在总仓库根目录构建并启动 8079 本地服务后，运行：

```powershell
npm install --prefix projects/009-mirofish/.cache/qa --no-audit --no-fund --ignore-scripts playwright
node projects/009-mirofish/src/verify_ui.cjs
```

检查默认使用已安装的 Chrome 无界面实例，使用独立临时浏览器上下文，不连接用户个人浏览器资料。脚本与测试依赖仅用于开发，不进入网页发布包。

桌面截图存入 `assets/cover.png`；手机及完整桌面截图存入 `.cache/`。当前检查没有覆盖所有浏览器、真实手机设备与辅助阅读器。

## 没有验证的事项

没有启动原版 MiroFish 后端，没有调用 LLM / Zep / OASIS，没有进行预测回测、真人代表性验证、API 成本测量或规模性能测试。本记录只验证独立教学展示，不应解释为上游能力已全部实测。
